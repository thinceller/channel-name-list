version: 2
jobs:
  build:
    branches:
      only:
        - master
    docker:
      - image: circleci/node:8
    working_directory: ~/repo
    steps:
      - checkout
      - run: yarn
      - run:
          name: Deploy to firebase
          command: ./node_modules/.bin/firebaase deploy --token "$FIREBASE_TOKEN"
